<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f8ff;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        .profile-container {
            width: 90%;
            max-width: 400px;
            background: white;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            border-radius: 12px;
            overflow: hidden;
            text-align: center;
        }

        .profile-container .top img {
            width: 100%;
            height: 300px;
            object-fit: cover;
            border-bottom: 2px solid #00bcd4;
        }

        .profile-container .bottom {
            padding: 20px;
        }

        .profile-container .bottom h2 {
            font-size: 20px;
            color: #333;
            margin-bottom: 10px;
        }

        .profile-container .bottom h2:first-of-type {
            font-size: 24px;
            font-weight: bold;
            color: #00bcd4;
        }

        .profile-container .bottom h2:last-of-type {
            font-style: italic;
            color: #555;
        }

        @media (max-width: 480px) {
            .profile-container .top img {
                height: 200px;
            }

            .profile-container .bottom h2 {
                font-size: 18px;
            }
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <div class="top">
            <img src="<%= foundUser.profileImage %>" alt="<%= foundUser.username %>'s profile picture">
        </div>
        <div class="bottom">
            <h2><%= foundUser.username %></h2>
            <h2>Email: <%= foundUser.email %></h2>
            <h2>Bio: <%= foundUser.bio %></h2>
        </div>
    </div>
</body>
</html>
