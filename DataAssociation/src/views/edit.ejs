<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        html,
        body {
            height: 100%;
            width: 100%;
        }
    </style>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <button class="bg-red-500 px-2 py-2 text-sm rounded-md mb-3 cursor-pointer">Logout</button>
        <h1 class="name font-semibold text-3xl">Hello, <%= user.firstName %>
        </h1>
        <p>You can edit your <span class="text-blue-400">post</span></p>
        <form action="/user/edit/<%= post._id %>" method="post">
            <input class="w-1/3 my-4 block bg-transparent border-2 rounded-md border-zinc-600 p-2 outline-none"
                type="text" placeholder="Title" name="title" value="<%= post.title %>">
            <textarea
                class="w-1/3 block resize-none bg-transparent border-2 rounded-md border-zinc-600 p-3 outline-none"
                placeholder="what's on your mind" name="content">
                <%= post.content %>
            </textarea>
            <input type="submit" value="update post" class="mt-5 px-4 py-2 cursor-pointer bg-yellow-500 text-black rounded-md">
        </form>
    </div>
    </div>
</body>

</html>